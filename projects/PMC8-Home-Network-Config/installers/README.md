# Installers

The v1.0 executable is distributed as a GitHub Release asset instead of being committed into git:

https://github.com/ExploreScientific/OpenGOTO-Community-Projects/releases/tag/home-network-config-v1.0

Use `checksums/SHA256SUMS.md` to verify the mirrored release asset.
